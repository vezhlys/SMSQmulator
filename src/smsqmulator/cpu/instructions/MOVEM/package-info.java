/**
 * These are replacement MOVEM instructions for SMSQmulator.
 * 
 * They are (supposed to be) a little faster than those from the m68k package by Tony Headford.
 * 
 * There are 12 classes, corresponding to the 6 source effective address modes for register to memory and the 6 source effective address modes for memory to register.
 * 
 */
package smsqmulator.cpu.instructions.MOVEM;
